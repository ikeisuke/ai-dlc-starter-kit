# デプロイチェックリスト

## デプロイ情報
- **バージョン**: v1.3.0
- **デプロイ予定日**: 2025-12-10
- **デプロイ方式**: GitHubリポジトリへのリリース（mainブランチへのマージ + 自動タグ作成）

## デプロイ前チェックリスト

### Construction Phase 完了確認
- [ ] すべてのUnitが完了している
- [ ] 各Unitの成果物が作成されている

### ドキュメント品質
- [ ] プロンプトファイルの構文エラーがない
- [ ] テンプレートファイルの整合性が取れている
- [ ] リンク切れがない

### バージョン管理（メタ開発特有）
- [ ] `version.txt` を `1.3.0` に更新
- [ ] setup-init実行（アップグレードモード）で `docs/aidlc/` を最新化

### ドキュメント更新
- [ ] README.md更新（変更履歴、新機能など）
- [ ] history.md に履歴を追記

## デプロイ手順

### 1. バージョン更新
```bash
echo "1.3.0" > version.txt
```

### 2. setup-init実行（アップグレードモード）
```bash
# setup-init.md を読み込み、セクション7（共通ファイルの配置）を実行
# rsync で prompts/package/ → docs/aidlc/ に同期
```

### 3. README.md更新
- 変更履歴セクションに v1.3.0 の変更内容を追記

### 4. 最終コミット
```bash
git add .
git commit -m "chore: Operations Phase完了 - v1.3.0リリース準備"
```

### 5. PR作成
```bash
gh pr create --base main --title "v1.3.0" --body "..."
```

### 6. PRマージ
- PRをレビュー・マージ

### 7. 自動タグ作成確認
- GitHub Actionsが `v1.3.0` タグを自動作成することを確認

## ロールバック手順

問題が発生した場合：

```bash
# 前のバージョンに戻す
git checkout v1.2.2
```

## デプロイ後チェックリスト
- [ ] GitHub上でタグ `v1.3.0` が作成されている
- [ ] リリースノートが必要であれば作成

## 備考

- このプロジェクトはドキュメント・テンプレートプロジェクトのため、サーバーやデータベースのデプロイは不要
- CI/CDは前回サイクル（v1.2.2）で構築済みの自動タグ付けワークフローを使用

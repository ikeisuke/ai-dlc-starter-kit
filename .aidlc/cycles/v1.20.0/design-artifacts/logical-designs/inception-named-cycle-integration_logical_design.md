# 論理設計: Inceptionプロンプト名前付きサイクル統合

## 概要

`prompts/package/prompts/inception.md` の具体的な変更箇所と変更内容の論理設計。

**重要**: このUnitはプロンプト（AI指示文書）の変更のみであり、コードは書かない。

## 変更箇所一覧

### 変更箇所1: ステップ5.5（L269-293）

**現状のL293**:
```text
**注意**: `cycle_mode` に基づくモード別分岐ロジック（名前入力フロー、ディレクトリパス組み立て等）はUnit 003で実装する。このステップでは値の読み取りとバリデーションのみ。
```

**変更**: L293の注記を削除し、以下のモード別分岐セクションに置換。

#### 追加内容

```markdown
**モード別分岐ロジック**:

**mode = "named" の場合**:

サイクル名の入力を求める:

\```text
【名前付きサイクルモード】（rules.cycle.mode = "named"）
サイクル名を入力してください（半角英小文字・数字・ハイフンのみ）。
例: waf, auth-system, feature-1
\```

**バリデーション手順**:
1. 正規表現チェック: `^[a-z0-9][a-z0-9-]{0,63}$`
2. 禁止名チェック: `backlog`, `backlog-completed` は予約語のため使用不可
3. `v[0-9]` 開始チェック: `^v[0-9]` で始まる名前はバージョンディレクトリと混同されるため使用不可

**バリデーション失敗時**: パターン別エラーメッセージを表示し、再入力を求める:

- 正規表現不一致:
  \```text
  【エラー】サイクル名 "[入力値]" は無効です。
  有効な形式: 半角英小文字・数字・ハイフンのみ（先頭は英小文字または数字、最大64文字）
  例: waf, auth-system, feature-1
  \```
- 禁止名衝突（backlog, backlog-completed）:
  \```text
  【エラー】サイクル名 "[入力値]" は予約語のため使用できません。
  別の名前を入力してください。
  \```
- `v[0-9]` 開始:
  \```text
  【エラー】サイクル名 "[入力値]" は "v" + 数字で始まるため、バージョンディレクトリと混同される可能性があります。
  別の名前を入力してください（例: feature-v2, ver2 など）。
  \```

**バリデーション成功時**: `cycle_name` をコンテキスト変数として保持し、ステップ6へ進む。

**mode = "ask" の場合**:

AskUserQuestion機能で選択肢を提示:

\```text
サイクルの種類を選択してください：
1. 名前付きサイクル（例: waf/v1.0.0 - 機能テーマごとに分類）
2. 通常サイクル（例: v1.0.0 - 従来形式）(推奨)
\```

「1. 名前付きサイクル」選択時: 上記 `mode=named` の名前入力フローへ
「2. 通常サイクル」選択時: `mode=default` と同じフローへ（名前入力なし）

**mode = "default" の場合**:

変更なし。従来のフローで動作する（名前入力なし）。
```

### 変更箇所2: ステップ6-1（L339-358）直近サイクル表示

**現状のL342**:
```bash
ls -d docs/cycles/v*/ 2>/dev/null | sort -V | tail -3
```

**変更**: `cycle_name` 設定時の分岐を追加。

```markdown
**直近サイクル表示**:

`cycle_name` が設定されている場合（名前付きサイクルモード）:
\```bash
ls -d docs/cycles/${cycle_name}/v*/ 2>/dev/null | sort -V | tail -3
\```

`cycle_name` が未設定の場合（従来）:
\```bash
ls -d docs/cycles/v*/ 2>/dev/null | sort -V | tail -3
\```
```

### 変更箇所3: ステップ6-2（L361-381）バージョン提案

**現状のL377-381**:
```text
1. `branch_version` が設定されている場合: そのバージョンを提案
2. そうでない場合: `suggested_*` から選択肢を提示し、「カスタム名を入力する」も選択肢に含める
3. 「カスタム名を入力する」が選択された場合: ユーザーに自由入力を求める
4. 選択または入力されたサイクル名が `all_cycles` に含まれる場合: エラー表示して再選択
```

**変更**: 名前付き時のバージョン提案ロジックを追加。

```markdown
**AIの判断フロー**:

**`cycle_name` が設定されている場合（名前付きサイクルモード）**:

1. `suggest-version.sh` を実行して `all_cycles` を取得
2. `all_cycles` のカンマ分割トークンから `${cycle_name}/v*` にマッチするものを抽出
3. マッチしたバージョンがある場合: 最新バージョンを基準にpatch/minor/majorを再計算して選択肢を提示
4. マッチしたバージョンがない場合（名前付きサイクルの初回）: `v1.0.0` を提案
5. ユーザーが選択または入力したバージョンで `{{CYCLE}}` を `${cycle_name}/${version}` 形式で組み立て
6. 組み立てた `{{CYCLE}}` が `all_cycles` のカンマ分割トークンに完全一致する場合: 「このサイクルは既に使用されています」エラーを表示して再選択

**`cycle_name` が未設定の場合（従来フロー）**:

1. `branch_version` が設定されている場合: そのバージョンを提案
2. そうでない場合: `suggested_*` から選択肢を提示し、「カスタム名を入力する」も選択肢に含める
3. 「カスタム名を入力する」が選択された場合: ユーザーに自由入力を求める（例: `feature-auth`, `2026-03`）
4. 選択または入力されたサイクル名が `all_cycles` のカンマ分割トークンに完全一致する場合: エラー表示して再選択
```

### 変更箇所4: L78 継続プロンプト例

**現状**:
```text
以下のファイルを読み込んで、サイクル vX.X.X の Inception Phase を継続してください：
```

**変更**:
```text
以下のファイルを読み込んで、サイクル {{CYCLE}} の Inception Phase を継続してください：
```

## 変更不要だが確認する既存箇所

以下の箇所は `{{CYCLE}}` 変数をそのまま使用しており、名前付き形式（`waf/v1.0.0`）でも正常に動作する。実装時に変更不要であることを確認する。

| 箇所 | 行番号 | 内容 | 確認理由 |
|------|--------|------|----------|
| ステップ7 | L419, L431, L437, L455, L458 | `setup-branch.sh {{CYCLE}} branch/worktree` | Unit 002で名前付き対応済み |
| ステップ8 | L475, L478 | `docs/cycles/{{CYCLE}}/` 存在確認 | パスが自然に名前付き形式になる |
| ステップ9 | L492 | `init-cycle-dir.sh {{CYCLE}}` | Unit 002で名前付き対応済み |
| ステップ7出力例 | L464, L466 | `branch:cycle/v1.12.1` | 例示のため変更不要（名前付き時は `branch:cycle/waf/v1.0.0` になる） |
| 完了時PR作成 | L1044-1047 | `--title "サイクル {{CYCLE}}"` | 名前付き時 `サイクル waf/v1.0.0` となり問題なし |
| 履歴管理 | L21, L34 | `docs/cycles/{{CYCLE}}/history/inception.md` | パスが自然に名前付き形式になる |
| ラベル付与 | L574 | `label-cycle-issues.sh "{{CYCLE}}"` | ラベル名に名前付き形式が含まれる（許容） |

## NFR対応

### 後方互換性

- `mode=default` 時: ステップ5.5に分岐追加されるが、`mode=default` ブロックは「変更なし」のため既存動作と完全同一
- `{{CYCLE}}` 変数: 名前なし時は従来の `vX.Y.Z` 形式のまま

### セキュリティ

- サイクル名バリデーション: 正規表現 + 禁止名チェック + `v[0-9]`開始禁止
- パストラバーサル防止: `[a-z0-9-]` のみ許容のため `..` は自然に拒否

### スケーラビリティ

- 将来的なモード追加: `mode=xxx` ブロックを追加するだけで拡張可能

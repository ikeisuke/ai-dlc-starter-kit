# 既存コード分析 - v1.15.2

## 分析対象
Issue #194 で指摘された `prompts/package/` 配下の全ファイル

## 分析結果

### 総合サマリ

Issue #194 の must 11件のうち **6件が誤検出（false positive）** であることが判明。
レビューツール（keelai）がソースコード中の `$$`、`$'\n'`、`$` のレンダリングを誤った可能性が高い。

| 分類 | 全件数 | 確認済み | 誤検出 | 部分的確認 |
|------|--------|---------|--------|-----------|
| must (critical) | 11 | 2 | 6 | 2 (※1) |
| want (改善推奨) | 4 | 4 | 0 | 0 |
| imo (参考) | 3 | 3 | 0 | 0 |
| **合計** | **18** | **9** | **6** | **2** |

※1: 部分的確認は、Issue記載内容と実際のコードに差異があるが、修正すべき問題は存在するケース

### 詳細

#### 誤検出（修正不要）- 6件

| # | 対象ファイル | 指摘内容 | 実際のコード | 判定理由 |
|---|------------|---------|------------|---------|
| 1 | `bin/aidlc-cycle-info.sh:56` | grep正規表現が行をまたいでいる | `grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$'` - 1行で正しく記述済み | レビューツールの表示の問題 |
| 3 | `bin/ios-build-check.sh:49` | `$` 検出パターンが不正 | `*'$'*` - シングルクォートで正しく記述済み | レビューツールの表示の問題 |
| 4 | `bin/migrate-backlog.sh:168` | `\n'` 構文が壊れている | `$'\n'` - ANSI-C quotingで正しく記述済み（実際は174行目付近） | レビューツールの表示の問題 |
| 5-8 | `bin/read-config.sh:100,157,180,204` | 一時ファイル名の `$` 展開問題 | 4箇所すべて既に `$$` を正しく使用済み | レビューツールの表示の問題 |

#### 確認済み（修正必要）- 9件

| # | 対象ファイル | 指摘内容 | 確認状況 |
|---|------------|---------|---------|
| 2 | `bin/check-open-issues.sh:32` | `--limit` 数値バリデーションなし | 確認: `LIMIT="$2"` で検証なし |
| 9 | `bin/suggest-version.sh:78` | case文にdefaultケースなし | 確認: `*)` 分岐がない |
| 12 | `bin/check-open-issues.sh:56` | エラー処理フォールバック不十分 | 確認: 空エラー時の詳細なし |
| 13 | `bin/issue-ops.sh:158` | `parse_gh_error` 認証エラー未対応 | 確認: "not found" パターンのみ |
| 14 | `bin/cycle-label.sh:103` | リダイレクト順序が直感的でない | 確認: コメントはあるが改善の余地あり |
| 15 | `bin/setup-branch.sh:56` | `realpath` 推奨 | 確認: 手動パス解決を使用 |
| 16 | `bin/aidlc-git-info.sh:18` | IFS初期化推奨 | 確認: `set -uo pipefail` のみ |
| 17-18 | `bin/env-info.sh:103,120,205` | `cat \| dasel` → `dasel -f` 推奨 | 確認: 3箇所でcat使用（Issue記載は2箇所だが実際は3箇所） |

#### 部分的確認（修正必要だが Issue 記載と差異あり）- 2件

| # | 対象ファイル | 指摘内容 | 実際の状況 |
|---|------------|---------|-----------|
| 10 | `guides/ios-version-update.md:39` | パラメータ展開構文エラー `${{CYCLE}#v}` | 確認: テンプレート変数 `{{CYCLE}}` と bash パラメータ展開 `${...#v}` が混在。`${CYCLE#v}` に修正すべき（テンプレート置換後の正しいbash構文として） |
| 11 | `guides/config-merge.md:144` | TOML同一テーブル重複定義 | 確認: Issue記載は `[rules.mcp_review]` だが実際は `[rules.reviewing]` の重複。ドキュメント例の問題 |

### 行番号のズレ

一部のIssue記載行番号と実際の行番号にズレあり:
- #4: Issue記載168行目 → 実際は174行目付近
- #17-18: Issue記載106,227行目 → 実際は103,120,205行目

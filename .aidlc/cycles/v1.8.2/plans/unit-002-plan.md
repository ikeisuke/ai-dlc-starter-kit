# Unit 002 計画: セットアップスクリプト修正

## 概要

セットアップスクリプト（`prompts/setup-prompt.md`）を修正し、スキルファイルのコピーとシンボリックリンク作成を自動化する。

## 変更対象ファイル

| ファイル | 変更内容 |
|----------|----------|
| `prompts/setup-prompt.md` | rsync対象にskillsを追加、シンボリックリンク作成処理を追加 |

## 実装計画

### Phase 1: 設計

1. **ドメインモデル設計**
   - スキルファイル配布の概念モデルを定義
   - セットアップフローの変更点を整理

2. **論理設計**
   - rsync同期の追加処理
   - シンボリックリンク作成の処理フロー
   - エラーハンドリング

### Phase 2: 実装

1. **rsync対象の追加**
   - セクション8.2にskillsディレクトリの同期処理を追加
   - 既存のtemplates/guides/binと同様の形式

2. **シンボリックリンク作成処理の追加**
   - `.claude/skills/` から `docs/aidlc/skills/` へのリンク作成
   - 既存リンクの処理（上書き確認）

3. **完了メッセージの更新**
   - セクション10の作成ファイル一覧にskillsを追加

## 完了条件チェックリスト

- [x] rsync対象に `skills/` ディレクトリを追加
- [x] `.claude/skills/` から `docs/aidlc/skills/` へのシンボリックリンク作成処理を追加
- [x] 既存のシンボリックリンクがある場合の処理（上書き or スキップ）を実装

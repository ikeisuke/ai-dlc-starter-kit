# Unit: doctor 完全診断の SoT ドキュメント反映 + 用語整合

## 概要

doctor が `[phase]` / `[trace]` を含む 11 領域の完全診断になったことを、SoT ドキュメント（`skills/aidlc-v3/steps/doctor.md` / `docs/v3/workflow.md` / `docs/v3-renewal-plan.md`）へ反映する。「alpha.8 defer」注記を「実装済み」へ更新し、領域カウント表記を「11 領域」で統一する。出力例を実装の実出力に揃える。

## 含まれるユーザーストーリー

- ストーリー 4: doctor 完全診断の SoT ドキュメント反映

## 責務

- `skills/aidlc-v3/steps/doctor.md`: 「9 領域」→「11 領域」、診断領域テーブルに `[phase]` / `[trace]` 追加、出力例に 2 行追加、末尾「## alpha.8 defer」セクションを実装済み記述へ置換、`[trace]` と `[work-items]` の役割分担を明示。
- `docs/v3/workflow.md`: §3.6（段階スコープ注記 `:160-161` / チェック項目テーブル `:176-177` / 出力例 `:195-200` / コマンド体系 `:31`）の「alpha.8 defer」を「実装済み」へ更新。
- `docs/v3-renewal-plan.md`: doctor セクション（`:905` / `:917-918` / `:940-944`）と Phase 6 完了条件（`:1092`）の「alpha.8 defer」を「実装済み」へ更新。
- 公開ドキュメント（`doctor.md` / `workflow.md` / `docs/v3-renewal-plan.md`）の領域カウント表記の用語整合（「11 領域」統一 / 「8 領域 + parse-guard」「9 領域」の揺れ解消）。`doctor.sh` ヘッダコメント自体のカウント更新は Unit 001 の責務（本 Unit はコード非編集）。

## 境界

- `doctor.sh` / `test-doctor.sh` のコード変更は **Unit 001 の責務**（本 Unit はドキュメントのみ。ただし `doctor.sh` ヘッダ内カウントは Unit 001 で更新済み前提）。
- フェーズ導出規則・size×depth 規則そのものの記述変更はしない（doctor の段階スコープ注記のみ更新）。

## 依存関係

### 依存する Unit

- 001-doctor-phase-trace-areas（依存理由: 出力例を実装の実出力に揃える必要があり、段階スコープ注記の「実装済み」化は実装完了を前提とするため）

### 外部依存

- なし（ドキュメント編集のみ）

## 非機能要件（NFR）

- **パフォーマンス**: 該当なし（ドキュメント）。
- **セキュリティ**: 該当なし（公開ドキュメント / ローカル絶対パス・機密情報を含めない）。
- **スケーラビリティ**: 該当なし。
- **可用性**: 該当なし。

## 技術的考慮事項

- markdown_lint=true のため markdownlint 整合を維持。
- 出力例の severity トークン位置を Unit 001 の実装出力と一致させる。
- SoT 二重定義回避（`data-model.md §5` が導出規則の正本 / workflow・renewal-plan は参照のみ）の原則を崩さない。

## 関連Issue

- #741（doctor に `[phase]` / `[trace]` 領域を追加 / 受け入れ基準「SoT の alpha.8 defer 注記を実装済みに更新」）
- Epic: #736（v3 リニューアル / Phase 6 完了条件の明示更新）

## 実装優先度

High

## 見積もり

小（ドキュメント 3 ファイル + 用語整合 / 0.5 セッション程度）

---
## 実装状態

有効値: 未着手 | 進行中 | 完了 | 取り下げ

- **状態**: 完了
- **開始日**: 2026-07-01
- **完了日**: 2026-07-01
- **担当**: -
- **エクスプレス適格性**: -
- **適格性理由**: -

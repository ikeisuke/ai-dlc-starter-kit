# 論理設計: リモートデフォルトブランチ取り込み確認

## 概要

01-setup.mdのステップ10-3のbehind時メッセージを拡張する。既存のsetup-branch.sh出力を活用し、新規スクリプトやロジック追加は行わない。

**重要**: この論理設計では**コードは書かず**、コンポーネント構成とインターフェース定義のみを行います。

## アーキテクチャパターン

既存のパイプライン（setup-branch.sh → main_status出力 → 01-setup.md表示）を維持。表示レイヤーのみ変更。

## コンポーネント構成

```text
skills/aidlc/
├── scripts/
│   └── setup-branch.sh        # [変更なし] main_status出力済み
└── steps/
    └── inception/
        └── 01-setup.md        # [変更] ステップ10-3のテーブル行更新
```

## 処理フロー

### ステップ10-3（変更後）

1. `setup-branch.sh`の出力から`main_status:`行をパース（既存）
2. main_statusに応じた表示（メッセージのみ変更）:
   - `up-to-date`: 「最新です」（変更なし）
   - `behind`: 「⚠ リモートデフォルトブランチに未取り込みコミットがあります。作業開始前に最新変更を取り込むことを推奨します（git merge/rebase）。」（**変更対象**）
   - `fetch-failed`: 既存メッセージを維持（変更なし）
3. フェーズのブロックは行わない（警告のみ、既存動作維持）

## 技術選定

- **言語**: Markdown（プロンプトファイルのみ）
- **ツール**: 既存のsetup-branch.shを活用

## 不明点と質問

（なし）

# Unit: aidlc-setup 01-detect の独立チェック指針明示

## 概要

`skills/aidlc-setup/steps/01-detect.md` セクション1「早期判定」の 3 条件チェックが自然言語のみで記述されており、AI エージェントが `&&` / `||` チェーンで束ねた結果 v1 残骸の検出が漏れる事故（#600、ikeisuke/norigoro で発生）が起きた。本 Unit では独立評価の指針と具体コマンド例を追記する。

## 含まれるユーザーストーリー

- ストーリー 4: aidlc-setup 01-detect が複数条件を独立評価する

## 責務

- `skills/aidlc-setup/steps/01-detect.md` セクション1 に、3 条件を独立に `test -f` 等で評価する具体コマンド例を追加する
- `&&` / `||` チェーン禁止の注意書きを同セクションに追加する
- 既存の CASE_1 / CASE_2 / CASE_3 分類ロジックは変更しない（記述追加のみ）
- ikeisuke/norigoro で発生した誤判定事例を短い reference として記述する

## 境界

- 判定ロジック自体（CASE 分類）の変更は行わない
- 既存の `01-detect.md` 他セクションや、他の aidlc-setup ステップには変更を加えない
- シェルスクリプトの新規追加・既存スクリプトの挙動変更は行わない

## 依存関係

### 依存する Unit

- なし

### 外部依存

- なし（ドキュメント改訂のみ）

## 非機能要件（NFR）

- **パフォーマンス**: 該当なし
- **セキュリティ**: 該当なし
- **スケーラビリティ**: 該当なし
- **可用性**: 既存の「早期判定」を読んでいた利用者にとって追加情報として機能する（既存記述を削除・破壊しない）

## 技術的考慮事項

- 追記位置はセクション1「早期判定」内、既存の 3 条件リストの直後に「独立チェックの実装指針」小見出し（または同等の注意書きブロック）を設ける
- `test -f` 以外にも `[ -f ... ]` や `ls ... 2>/dev/null` 等の書式が使われうるため、代表例を 1〜2 個に絞って明示
- ikeisuke/norigoro の事例は再現手順レベルまで書く必要はなく、「`.aidlc/` 不在時に `&&` 短絡評価で後続スキップ → v1 検出漏れ」を 1〜2 行で記述

## 関連Issue

- #600

## 実装優先度

Medium

## 見積もり

手順書改訂のみで 0.25〜0.5 日規模

---
## 実装状態

有効値: 未着手 | 進行中 | 完了 | 取り下げ

- **状態**: 完了
- **開始日**: 2026-04-25
- **完了日**: 2026-04-25
- **担当**: Claude (Opus 4.7)
- **エクスプレス適格性**: -
- **適格性理由**: エクスプレスモード無効（通常フロー）

# Unit: /aidlc version アクションの追加

## 概要
`/aidlc version`（短縮形: `v`）でスキルのバージョンを表示するアクションを追加する。

## 含まれるユーザーストーリー
- ストーリー 4: versionアクション追加 (#508)

## 責務
- SKILL.mdのARGUMENTSパーシングに `version`（短縮形: `v`）を追加
- 引数ルーティングテーブルに `version` を追加
- ヘルプ表示テーブルに `version` を追加
- version表示ロジックの実装（共通初期化フローは実行しない）
- `v` エイリアスが既存アクション短縮形（`i`/`c`/`o`/`e`/`h`）と競合しないことを確認

## 境界
- version表示以外のコマンド体系再設計は行わない
- `env-info.sh` の変更は行わない（既存の `starter_kit_version` 取得ロジックを活用）

## 依存関係

### 依存する Unit
- なし

### 外部依存
- なし

## 非機能要件（NFR）
- なし

## 技術的考慮事項
- `env-info.sh` の出力から `starter_kit_version` を取得する、または `read-config.sh` で直接読む
- 共通初期化フローを実行せず、バージョン表示のみで終了する

## 関連Issue
- #508

## 実装優先度
High

## 見積もり
小規模（SKILL.md に数行追加）

---
## 実装状態

- **状態**: 完了
- **開始日**: 2026-04-02
- **完了日**: 2026-04-02
- **担当**: @ikeisuke
- **エクスプレス適格性**: -
- **適格性理由**: -

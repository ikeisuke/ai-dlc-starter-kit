# 既存コード分析 - v1.14.1

## 変更対象ファイル

### 1. prompts/package/prompts/operations.md

#### #185: ステップ6.0 バージョンファイル更新（591-623行目）

- `project.name = "ai-dlc-starter-kit"` の場合のみ実行されるプロジェクト固有ロジック
- version.txt の更新（vプレフィックス除去）
- docs/aidlc.toml の `starter_kit_version` 更新（sed コマンド）
- **移動先**: `docs/cycles/rules.md` のカスタムワークフローセクション

#### #184: ステップ0 変更確認（345-372行目）

- 「いいえ」選択時: ステップ1-5を全スキップし、ステップ6へ
- 例外: `rules.md` のカスタムワークフローはスキップ対象外（372行目に注記あり）
- **変更**: スキップ対象をステップ1-4に限定、ステップ5は常に実行

#### #184: ステップ1 デプロイ準備（374-473行目）

- バージョン確認ロジックを含む（381-470行目）
  - iOSプロジェクト向け特別処理（385-434行目）
  - 通常のバージョン確認（446-470行目）
- **変更**: バージョン確認をステップ6（リリース準備）に移動

#### ステップ5 バックログ整理（509-568行目）

- ステップ1-4への直接的な依存なし
- `backlog_mode` 設定を参照するが、これはaidlc.tomlの設定でありステップ1-4の成果物ではない
- **変更**: スキップ不可にする

#### ステップ6 リリース準備（570-838行目）

- 11のサブステップ（6.0-6.7）で構成
- 6.0: バージョンファイル更新（削除対象）
- **変更**: バージョン確認ロジックをステップ1から受け入れる位置を特定する必要あり

### 2. prompts/package/prompts/construction.md

#### #182: unit_branch.enabled 参照箇所（385-387行目）

- 現在のロジック（387行目）:
  - `enabled = false` の場合: スキップ
  - `enabled = true`、未設定、または不正値の場合: 実行
- **変更**: デフォルトを反転
  - `enabled = true` の場合: 実行
  - `enabled = false`、未設定、または不正値の場合: スキップ
- construction.md のみに参照あり（他のプロンプトファイルには参照なし）

### 3. docs/cycles/rules.md

- カスタムワークフローセクションが既に存在（33-42行目）
- 現在は `/upgrading-aidlc` の実行指示のみ
- **変更**: ステップ6.0のバージョンファイル更新ロジックを追加

## 影響範囲

| 変更 | 影響ファイル | 影響範囲 |
|------|------------|---------|
| #185 | operations.md, rules.md | スターターキット固有。他プロジェクトには影響なし |
| #184 | operations.md | 全プロジェクトのOperations Phase |
| #182 | construction.md | 全プロジェクトのConstruction Phase（unit_branch未設定時のみ） |

# 論理設計: Unit 005 /aidlc 委譲フロー Skill ツール経由自動継続実行規約化

## ステップ 0: 事前コード読込み

> 適用条件: depth_level != minimal の場合のみ必須。minimal は設計ステップ自体スキップ可のため N/A。

### (a) Read 対象ファイル + 目的

| ファイル | 目的 |
|---------|------|
| `skills/aidlc/SKILL.md` 「### 独立フロー委譲」セクション | 改修対象、既存記法スタイル確認 |

### (b) 設計時に意識すべき挙動

- 既存セクションは「委譲手順」「委譲先テーブル」の 2 ブロック構成
- 改修は 3 ブロック構成に拡張: (a) Skill ツール経由 invoke 規約 + (b) フォールバック仕様 + (c) 委譲先テーブル（既存維持）

### (c) 既存実装に基づく代替案検討

- **採用**: 既存セクション内の構造拡張 (3 ブロック構成)
- **却下**: 新規 sub-section を別ファイルに分離 → SKILL.md 内 SoT 探索コスト増

## 概要

SKILL.md 「### 独立フロー委譲」セクションを 3 ブロック構成 (Skill ツール経由 invoke 規約 / フォールバック仕様 / 委譲先テーブル) に拡張。

## アーキテクチャパターン

- **規範記述パターン**: AI エージェント挙動の規約として記述。実装コードは生成せず
- **二重防御パターン**: Skill ツール経由 invoke (primary) + テキスト案内 fallback の 2 経路

## コンポーネント構成

```text
skills/aidlc/SKILL.md
└── ### 独立フロー委譲   # 改修対象セクション (3 ブロック構成に拡張)
    ├── ブロック A: Skill ツール経由 invoke 規約 (新規)
    ├── ブロック B: フォールバック仕様 (新規 / 発火条件 + 復帰動作 1 節)
    └── ブロック C: 委譲先テーブル (既存維持)
```

## インターフェース設計

### ブロック A: Skill ツール経由 invoke 規約

- **適用範囲（検証状況に基づく）**:
  - **Claude Code**: 本サイクル中に Skill ツール連鎖呼び出し動作を実証済み（必須適用）
  - **Codex CLI**: Operations Phase 振り返り前段で検証予定（任意適用 / 検証結果は記録）
  - **Gemini CLI**: 環境未整備、検証範囲外（記録のみ、規約の必須適用対象外 / 将来拡張）
- AI エージェントが `/aidlc {action}` を受信した際、テキスト案内を介さず Skill ツールで `aidlc-{action}` スキルを invoke する
- `additional_context` (`/aidlc {action} {ctx}` の `{ctx}` 部分) は委譲先スキルに透過渡しする
- **成功時出力契約（責務境界 固定）**:
  - **最終応答主体**: 委譲先スキル単独 (`aidlc-{action}` の出力)
  - **親スキル責務**: 委譲先 invoke のみ。invoke 後の追加メッセージ出力は禁止（二重応答抑止）
  - **順序**: 親スキル invoke → 委譲先応答 (この 1 順序のみ)
  - **重複出力抑止**: 親スキルは「実行済み報告」も出力しない（委譲先応答が最終出力）

### ブロック B: フォールバック仕様（1 節固定 / 発火条件 + 責務 + 出力 + 復帰）

- **発火条件**: (a) Skill ツール未提供 / (b) 1 回目呼び出しが構造的失敗（ツール not found / 権限エラー / 即時エラー応答）
- **親スキル責務**: 1 回目呼び出し試行のみ。失敗時は再試行せず即フォールバックに降格
- **fallback 出力**:
  - `additional_context` 非空時: `「/aidlc-{action} {additional_context}」を実行してください。`（半角スペース 1 個区切り）
  - `additional_context` 空時: `「/aidlc-{action}」を実行してください。`（余分なスペース / 末尾空白なし）
- **復帰条件・タイミング**: 次ターン以降、ユーザーが再度 `/aidlc {action}` を入力した場合は通常通り `skill_tool` モード（ブロック A）から開始する。フォールバック降格は **その 1 ターン内のみの局所的判定**であり、永続的なモード変更ではない

### ブロック C: 委譲先テーブル (既存維持)

```text
| action | 委譲先スキル |
|--------|------------|
| `setup` | `/aidlc-setup` |
| `migrate` | `/aidlc-migrate` |
| `feedback` | `/aidlc-feedback` |
| `retrospective` | `/aidlc-retrospective` |
```

## 処理フロー概要

1. AI エージェントが `/aidlc {action}` を受信（`action ∈ {setup, migrate, feedback, retrospective}`）
2. 親スキルが委譲先 (`aidlc-{action}`) を ブロック C のテーブルで解決
3. ブロック A: Skill ツールで `aidlc-{action}` を 1 回 invoke 試行
4. 成功 → 親スキルは invoke のみ実行し、委譲先スキルが単独で最終応答（親スキルの追加出力なし）
5. 失敗 → ブロック B: fallback メッセージ出力 + ユーザー再入力待ち

## 非機能要件 (NFR)

- パフォーマンス: 既存テキスト案内経路と比較してユーザー操作 1 ステップ削減
- 可用性: フォールバック降格により AI エージェント側 Skill ツール非対応でも動作継続

## 技術選定

- 規約変更のみ（SKILL.md Markdown 編集）。スクリプト・コード変更なし

## 実装上の注意事項

- SKILL.md は 500 行制限。既存セクション (line 178-191 周辺) を 3 ブロック構成に置換するが、十分余裕あり
- 委譲廃止案 (#717 提案 2) の不採用判断は本 Unit の計画書および Issue #717 内に閉じる（SKILL.md 本体には記載しない / 責務分離）
- 本リポジトリ規約: Bash ツール引数文字列にコマンド置換 `$(...)` / backtick を含めない

# Unit 001 計画: migrate-config.sh バグ修���

## 概要
`migrate-config.sh` の3つの問題を修正する。

## 変更対象ファイル
- `skills/aidlc/scripts/migrate-config.sh`

## 問題と修正計画

### 問題1: awk失敗時も成功メッセージが出力される
- **箇所**: L218-222
- **原因**: `echo "migrate:add-key:rules.reviewing.tools"` がawk成功/失敗に関わらず実行される
- **修正**: 成功メッセージをawk成功時のみ出力するよう条件分岐

### 問題2: ヘッダコメントのexit code 2が実装と乖離
- **箇所**: L22
- **原因**: v1.27.3でexit 2→exit 0に変更されたが、ヘッダ���メントが未更新
- **修正**: ヘッダコメントからexit code 2の記述を削除し、現在の動作（警告あってもexit 0）を反映

### 問題3: `_has_warnings` 変数が未使用
- **箇所**: L33, L134, L220, L250, L277, L295
- **原因**: exit 2廃止後も変数の設定だけが残っ���いる
- **修正**: exit-code-convention.mdの参考実装パターンに従い、`_has_warnings` を活用して終了時にサマリメッセージを出力する

## 完了条件チェックリスト
- [ ] awk失敗時に成功メッセージが出力されない
- [ ] ��ッダコメントが実装と一致する
- [ ] `_has_warnings` が適切に使用される（終了時サマリ出力）
- [ ] 部分失敗時に失敗した項目が警告として出力される

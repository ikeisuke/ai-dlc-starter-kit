# Unit: rsync個別許可ルール削除

## 概要
`ai-agent-allowlist.md` からrsync個別許可パターンを削除し、スクリプト経由実行で許可が代替される旨の説明を追加する。

## 含まれるユーザーストーリー
- ストーリー 1: rsync個別許可ルールの削除

## 責務
- `ai-agent-allowlist.md` のallow配列からrsync関連の3行を削除
- スクリプト経由での実行で許可が代替される旨の説明を追加

## 境界
- `sync-package.sh` や `aidlc-setup.sh` のスクリプト本体は変更しない
- rsyncの実行ロジック自体には手を加えない

## 依存関係

### 依存する Unit
- なし

### 外部依存
- なし

## 非機能要件（NFR）
- **パフォーマンス**: 該当なし（ドキュメント変更のみ）
- **セキュリティ**: rsyncコマンドへの直接アクセス許可を排除
- **スケーラビリティ**: 該当なし
- **可用性**: 該当なし

## 技術的考慮事項
- rsyncは既に `sync-package.sh`（L105）と `aidlc-setup.sh`（L135 `_has_file_diff()`）内に閉じ込められている
- スクリプト実行の許可ルール（`Bash(docs/aidlc/skills/aidlc-setup/bin/aidlc-setup.sh:*)`等）があればrsync個別許可は不要

## 実装優先度
High

## 見積もり
小（ドキュメント1ファイルの編集）

## 関連Issue
- #367

---
## 実装状態

- **状態**: 完了
- **開始日**: 2026-03-20
- **完了日**: 2026-03-20
- **担当**: AI

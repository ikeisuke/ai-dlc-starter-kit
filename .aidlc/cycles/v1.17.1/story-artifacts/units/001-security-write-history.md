# Unit: write-history.shセキュリティ強化

## 概要
write-history.shの--contentパラメータのインジェクション対策と、review-flow.md内の呼び出し例をヒアドキュメント方式に統一する。後続Unitでwrite-history.sh呼び出しを追加するため、安全な呼び出しパターンを先に確立する。

## 含まれるユーザーストーリー
- ストーリー 9: write-history.shのエスケープ確認 (#236)

## 関連Issue
- #236

## 責務
- write-history.sh内部の--content処理における特殊文字の安全な処理を確認
- review-flow.md内のwrite-history.sh呼び出し例をシングルクォートのヒアドキュメント方式に統一
- インジェクションパターンが無効化されることの確認

## 境界
- write-history.sh以外のスクリプトのセキュリティ確認は含まない
- write-history.shの機能追加は含まない（セキュリティ確認と呼び出し例の修正のみ）

## 依存関係

### 依存する Unit
- なし（依存する他のUnitがない場合）

### 外部依存
- なし

## 非機能要件（NFR）
- **セキュリティ**: 特殊文字（`'`, `"`, `` ` ``, `$`, `;`等）を含む入力がインジェクションを引き起こさないこと

## 技術的考慮事項
- write-history.shはシェル引数として値を受け取るため、呼び出し側のクォーティングが防御の主な手段
- `$(cat <<'EOF'...EOF)` 方式でシングルクォートのヒアドキュメントを使用
- 変更対象: prompts/package/docs/review-flow.md, prompts/package/bin/write-history.sh

## 実装優先度
High

## 見積もり
小規模（確認作業 + 呼び出し例の修正）

---
## 実装状態

- **状態**: 完了
- **開始日**: 2026-02-28
- **完了日**: 2026-02-28
- **担当**: @ai

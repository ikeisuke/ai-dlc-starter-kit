# ドメインモデル: Unit 001 SKILL.mdパス解決ルール修正

## 概念モデル

パス解決ルール = SKILL.md制約事項セクション内のディレクトリプレフィックス定義。
AIエージェントがスキル内リソースパスを解決する際の基準。

## 現状
- 対象プレフィックス: `steps/`, `scripts/`（2種）

## 修正後
- 対象プレフィックス: `steps/`, `scripts/`, `config/`, `templates/`, `guides/`, `references/`（6種）

## 影響範囲
- SKILL.md L238（パス解決ルール）のみ
- テンプレート参照（L237）は既に `templates/` を個別記載 → パス解決ルールに統合しても矛盾なし

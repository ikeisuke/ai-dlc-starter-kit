# Unit 005 ドメインモデル: review-flow.md の MD038 違反 3 件の修正

## 概要

本 Unit はソースコードの追加・変更を含まない**ドキュメント記法のみの修正**であるため、本ドメインモデル
ドキュメントは「修正対象の規約表現」と「規約読者」を最小限のドメイン要素として記述する。Phase 2 で扱う
実装対象もコード生成ではなく、`skills/aidlc/steps/common/review-flow.md` の MD038 違反箇所 3 件の記法
書き換えに限定される。

## ドメイン要素

### エンティティ

| 名称 | 責務 |
|------|------|
| `review-flow.md` 内の「`内容` 列パス記法（規約）」 | review-summary の「内容」列で複数パスを記載する際の記法ルールを定義する文書要素 |
| 規約読者（AI エージェント / 人間レビュアー） | 上記規約を読み取り、review-summary 記載時に従う主体 |

### 値オブジェクト

| 名称 | 表現 |
|------|------|
| パストークン | repo-relative の path を backtick で囲んだコード span（例: `` `skills/aidlc/scripts/...` ``） |
| 区切り表現（修正後） | 平文の `,` + 半角スペース（コード span 化しない） |

### ドメインサービス

本 Unit にドメインサービスは存在しない（記法書き換えのみ）。

## 不変条件（修正後に維持されるルール）

1. **パスは backtick で囲む**: repo-relative path をコード span として記載する
2. **区切りは平文**: パス間の区切り（カンマ + 半角スペース）はコード span 化しない
3. **抽出側との契約維持**: review-summary 抽出正規表現 ``` `([^`]+)` ``` がパス span のみを取得する
   状態を維持する（区切り文字が抽出対象に混入しない）
4. **markdownlint MD038 で 0 件**: `review-flow.md` 単体の lint で MD038 違反 0 件を満たす

## 外部システム / 依存コンテキスト（非変更）

| 名称 | 役割 | 本 Unit での扱い |
|------|------|----------------|
| review-summary 抽出側ロジック | 既存の正規表現 ``` `([^`]+)` ``` で backtick 内のパスを取り出す技術実装 | 非変更（本ドメイン外の依存コンテキスト。修正後の規約はこの抽出ロジックの契約を維持する） |
| 新領域判定 | 抽出されたパス集合を入力に新領域指摘を判定するロジック | 非変更（抽出側ロジックの出力に依存） |

## 境界（含まないもの）

- 規約意味そのものの変更（記法表現の修正のみ）
- 他ファイルの MD038 違反対応
- review-summary 抽出ロジック本体の変更
- 抽出側正規表現の変更

## 参照

- Unit 定義: `.aidlc/cycles/v2.6.3/story-artifacts/units/005-review-flow-md038-fix.md`
- 計画: `.aidlc/cycles/v2.6.3/plans/unit-005-plan.md`
- 修正対象: `skills/aidlc/steps/common/review-flow.md` の line 121 / 122 / 283
- 関連 Issue: #705

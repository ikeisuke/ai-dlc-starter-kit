# Unit: aidlc-setupバージョン比較修正

## 概要
aidlc-setupスキルの01-detect.mdが`scripts/read-config.sh`を参照するが、aidlc-setupの`scripts/`には存在しないバグを修正する。

## 含まれるユーザーストーリー
- ストーリー 1: aidlc-setupバージョン比較の正常動作 (#516)

## 責務
- aidlc-setupスキルからバージョン比較に必要な設定値を取得できるようにする
- スキル間依存ルールを遵守した修正方法を選択・実装する

## 境界
- read-config.sh自体の機能変更は行わない
- aidlc-setupの他のステップ（02-generate-config.md等）は対象外

## 依存関係

### 依存する Unit
- なし

### 外部依存
- dasel（TOML解析ツール）

## 非機能要件（NFR）
- **パフォーマンス**: 該当なし
- **セキュリティ**: 該当なし
- **スケーラビリティ**: 該当なし
- **可用性**: 該当なし

## 技術的考慮事項
- 対応方法の候補:
  1. aidlc-setup側に`read-config.sh`のコピーまたはラッパーを配置
  2. 01-detect.mdのプロンプト内で代替手段を使用（dasel直接呼び出し等）
  3. aidlc-setupの`read-version.sh`を拡張して設定値取得に対応
- Construction Phaseの設計で最適な方法を決定する

## 関連Issue
- #516

## 実装優先度
High

## 見積もり
小規模（1ファイル修正 + 必要に応じてスクリプト追加）

---
## 実装状態

- **状態**: 未着手
- **開始日**: -
- **完了日**: -
- **担当**: -
- **エクスプレス適格性**: -
- **適格性理由**: -

# ドメインモデル: lib/ディレクトリrsync同期追加

## 概要

SYNC_DIRS配列に"lib"を追加するのみの変更。新しいエンティティや値オブジェクトの導入はなし。

## 既存構造への影響

### SYNC_DIRS配列（設定値）

- **変更**: 配列要素に"lib"を追加
- **影響範囲**: セットアップ時の同期対象ディレクトリが1つ増加
- **動作**: 既存の同期ループがそのまま処理する（ループロジック変更なし）

## ドメインイベント

該当なし（静的な設定変更のみ）

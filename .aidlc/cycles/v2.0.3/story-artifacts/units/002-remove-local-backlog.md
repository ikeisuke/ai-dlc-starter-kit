# Unit: ローカルバックログ廃止

## 概要
ローカルバックログの仕組み（git/git-onlyモード）を廃止し、GitHub Issue一本化する。設定・プロンプト・スクリプトを更新し、旧設定へのフォールバック＋警告を実装する。

## 含まれるユーザーストーリー
- ストーリー 2: ローカルバックログの廃止 (#423)

## 関連Issue
- #423

## 責務
- defaults.tomlの`backlog_mode`デフォルト値を`issue`に変更
- 有効値を`issue`/`issue-only`のみに制限
- 旧値`git`/`git-only`へのフォールバック＋警告実装（check-backlog-mode.sh）
- プロンプトファイルからローカルファイル操作の条件分岐削除
- agents-rules.mdのバックログ管理テーブル更新
- init-cycle-dir.shのバックログディレクトリ作成をスキップ
- migrate-detect.shのバックログディレクトリ検出ロジック更新
- 旧`git`/`git-only`を案内しているテンプレート・ガイド・設定例の更新（config.tomlコメント、setup-prompt.md等）

## 境界
- 既存の`.aidlc/cycles/backlog/`ディレクトリの自動削除は含まない
- 移行処理での読み取りは許可（既存資産検出のため）

## 依存関係

### 依存するUnit
- なし

### 外部依存
- なし

## 非機能要件（NFR）
- 該当なし

## 技術的考慮事項
- 旧設定利用者へのbreaking changeを避けるフォールバック方式
- 影響範囲が広い（複数プロンプト・スクリプト）

## 実装優先度
High

## 見積もり
中規模（設定変更＋複数プロンプト・スクリプト更新）

---
## 実装状態

- **状態**: 未着手
- **開始日**: -
- **完了日**: -
- **担当**: -
- **エクスプレス適格性**: -
- **適格性理由**: -

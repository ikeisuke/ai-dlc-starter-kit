# Unit: PRマージのユーザー判断化

## 概要

Operations Phase ステップ 7.13（PRマージ）で `automation_mode=semi_auto` 時もマージ実行前にユーザー確認を必須とする。マージは破壊的操作であり `AskUserQuestion使用ルール` の「ユーザー選択」に分類されるべき。

## 含まれるユーザーストーリー

- ストーリー 1: PRマージのユーザー判断化

## 責務

- **主変更**: `steps/operations/operations-release.md` のステップ 7.13 にマージ実行前の `AskUserQuestion` ゲートを追加（ユーザーが選ぶのはマージ実行の可否のみ。マージ方法は `merge_method` 設定から自動決定）
- **参照更新**: `steps/operations/02-deploy.md` のステップ 7.13 の記述をユーザー確認必須に合わせて更新（集約層としての参照のみ）
- **分岐明記**: `steps/operations/index.md` の分岐ロジックセクションにPRマージを「ユーザー選択」分類として明記

## 境界

- マージ方法の自動選択ロジック（`merge_method` 設定）は変更しない
- `pr-ops.sh merge` スクリプト自体の内部動作は変更しない
- `operations-release.sh merge-pr` のスクリプト動作は変更しない（呼び出しタイミングのみ変更）

## 依存関係

### 依存する Unit

- なし

### 外部依存

- なし

## 非機能要件（NFR）

- **パフォーマンス**: N/A（プロンプト変更のみ）
- **セキュリティ**: N/A
- **スケーラビリティ**: N/A
- **可用性**: N/A

## 技術的考慮事項

- 変更はステップファイル（markdown）のみ。スクリプト変更なし
- `AskUserQuestion` ではマージ実行の可否のみを問う。マージ方法・CI状態・PR番号は確認メッセージの情報提示として含める（選択肢ではない）

## 関連Issue

- #558

## 実装優先度

High

## 見積もり

小（ステップファイル2-3箇所の分岐変更）

---
## 実装状態

- **状態**: 完了
- **開始日**: 2026-04-11
- **完了日**: 2026-04-11
- **担当**: Claude
- **エクスプレス適格性**: -
- **適格性理由**: -

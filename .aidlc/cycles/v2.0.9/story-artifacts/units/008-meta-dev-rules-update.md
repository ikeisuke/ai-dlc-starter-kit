# Unit: メタ開発ルール定義の現行化

## 概要
`.aidlc/rules.md` のメタ開発セクションをv2.0.5以降の実態（skills/aidlcプラグイン構成）に合わせて更新する。既存Unit定義ファイルの技術的考慮事項も修正する。

## 含まれるユーザーストーリー
- （v2.0.8総点検で検出した乖離の修正）

## 責務
- `.aidlc/rules.md`「メタ開発の意識」セクションをv2.0.5以降の構成に更新
  - `prompts/package/` → `skills/aidlc/` への移行を反映
  - `docs/aidlc/` rsyncコピーの記述を削除（ディレクトリ自体が廃止済み）
  - プラグイン前提の構成原則を明記: プロジェクト相対パスでのスキル内リソース参照禁止
  - スキル間依存ルールを追加: 他スキルの内部実装への依存禁止（インターフェイス依存はOK）
- `.aidlc/rules.md`「ファイル参照境界ルール」の例外リスト更新
  - META-001: `prompts/package/**` → 削除または `skills/aidlc/` に更新
  - META-002: `docs/aidlc/**` → 削除（ディレクトリ廃止済み）
- 既存Unit定義ファイル（001〜007）の「技術的考慮事項」から古い `prompts/package/` 参照を修正

## 境界
- `.aidlc/rules.md` のメタ開発関連セクションのみ変更
- ルール以外のセクション（コーディング規約、セキュリティ要件等）は変更しない
- スキル本体のコード・ステップファイルは変更しない

## 依存関係

### 依存する Unit
- なし

### 外部依存
- なし

## 非機能要件（NFR）
- 該当なし（ルール文書の修正のみ）

## 技術的考慮事項
- `.aidlc/rules.md` はプロジェクトルートから直接参照される（スキル内リソースではない）
- 既存Unit定義ファイルの修正は軽微（技術的考慮事項の1行修正）

## 関連Issue
- なし

## 実装優先度
Medium

## 見積もり
小（ドキュメント修正のみ）

---
## 実装状態

- **状態**: 完了
- **開始日**: 2026-03-30
- **完了日**: 2026-03-30
- **担当**: -
- **エクスプレス適格性**: -
- **適格性理由**: -

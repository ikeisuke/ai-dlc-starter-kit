# デプロイチェックリスト

## デプロイ情報
- **バージョン**: v1.6.0
- **デプロイ予定日**: 2026-01-10
- **デプロイ環境**: GitHub リポジトリ（公開）
- **担当者**: ユーザー

## デプロイ前チェックリスト

### コード品質
- [x] すべてのUnit実装が完了している
- [ ] Construction Phaseで作成した成果物が整備されている
- [ ] README.mdが最新の情報に更新されている
- [ ] CHANGELOG.mdが更新されている（該当する場合）

### バージョン管理
- [x] version.txt が 1.6.0 に更新されている
- [ ] コミットメッセージが適切に記述されている
- [ ] サイクルブランチに全ての変更がコミットされている

### ドキュメント
- [ ] README.mdに今回の変更内容が追記されている
- [ ] 運用ドキュメントが整備されている
- [ ] プロンプト・テンプレートが更新されている（該当する場合）

### CI/CD
- [ ] GitHub Actionsが正しく設定されている
- [ ] 自動タグ付けワークフローが動作確認済み

## デプロイ手順

### 1. 事前準備
```bash
# 現在のバージョンを確認
cat version.txt

# 全ての変更がコミットされていることを確認
git status
```

### 2. PRの作成とマージ
```bash
# 現在のブランチからのPRを確認
CURRENT_BRANCH=$(git branch --show-current)
gh pr list --head "${CURRENT_BRANCH}" --state open

# PRが存在する場合、ドラフトPRをReady for Reviewに変更
gh pr ready

# PRタイトルを更新
gh pr edit --title "v1.6.0"

# レビュー後、PRをマージ（GitHub UI または gh コマンド）
gh pr merge --merge
```

### 3. 自動タグ付け確認
- mainブランチへのマージ後、GitHub Actionsが自動で `v1.6.0` タグを作成
- タグ作成を確認: `git tag -l "v1.6.0"`

### 4. 動作確認
- [ ] 新しいディレクトリでセットアップをテスト
- [ ] プロンプトが正しく動作することを確認

## ロールバック手順

問題が発生した場合の手順：

### 方法1: 前バージョンのタグから復旧ブランチを作成
```bash
# 前のバージョンタグから復旧ブランチを作成
git checkout -b hotfix/revert-v1.6.0 v1.5.4

# 必要な修正を行い、PRを作成してマージ
```

### 方法2: マージコミットをrevert
```bash
# マージコミットのSHAを確認
git log --oneline -5

# マージコミットをrevert（-m 1 はメインラインを指定）
git revert -m 1 <merge_commit_sha>

# 変更をpush
git push origin main
```

### 方法3: 参照用に前バージョンを確認（デタッチ状態）
```bash
# 前のバージョンを参照（デタッチ状態になるので注意）
git checkout v1.5.4

# 確認後、mainに戻る
git checkout main
```

## デプロイ後チェックリスト
- [ ] GitHub上でタグが作成されている
- [ ] README.mdの内容が正しく表示されている
- [ ] 新しい環境でセットアップが動作する

## 備考
- このプロジェクトはドキュメント・テンプレートプロジェクトのため、サーバーデプロイやデータベースマイグレーションは不要
- リリースはGitHubリポジトリへのプッシュとタグ作成で完了

## 不明点と質問

（対話を通じて不明点を明確化し、このセクションに記録していく）

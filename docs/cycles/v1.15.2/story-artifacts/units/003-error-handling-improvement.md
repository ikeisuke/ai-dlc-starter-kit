# Unit: エラー処理改善

## 概要
`issue-ops.sh`、`cycle-label.sh`、`setup-branch.sh` のエラー処理・可読性を改善する。

## 含まれるユーザーストーリー
- ストーリー 3: エラー処理の改善

## 責務
- `issue-ops.sh` の `parse_gh_error` 関数に認証エラーパターンを追加
- `cycle-label.sh` のリダイレクト設定の補足コメント改善
- `setup-branch.sh` の相対パス→絶対パス変換を `realpath` 利用に改善（フォールバック付き）

## 境界
- 上記3ファイル以外のスクリプトは対象外
- エラー処理の追加は成功系パスの既存動作に影響を与えないこと

## 依存関係

### 依存する Unit
- なし

### 外部依存
- GitHub CLI (`gh`)
- `realpath` コマンド（オプション、フォールバックあり）

## 非機能要件（NFR）
- **互換性**: 成功系・失敗系の既存動作を維持
- **可読性**: コメントの改善によりコードの意図が明確になること

## 技術的考慮事項
- 修正は `prompts/package/bin/` 配下で行う
- `realpath` は macOS にデフォルトでは存在しない場合がある。`realpath` 利用可能時はそれを使用し、利用不可時は既存の `cd + pwd` 方式にフォールバック
- 認証エラーパターンは "authentication", "401", "403" 等を検出

## 実装優先度
Medium

## 見積もり
小（修正箇所が明確）

## 関連Issue
- #194: 諸々調整（項番 #13, #14, #15）

---
## 実装状態

- **状態**: 完了
- **開始日**: 2026-02-19
- **完了日**: 2026-02-19
- **担当**: AI (Claude)

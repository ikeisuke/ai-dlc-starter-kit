# Unit: シェルスクリプトバグ修正・バリデーション強化

## 概要
`check-open-issues.sh` と `suggest-version.sh` の入力バリデーション追加およびエラー処理改善を行う。

## 含まれるユーザーストーリー
- ストーリー 1: シェルスクリプトのバグ修正・バリデーション強化

## 責務
- `check-open-issues.sh` の `--limit` オプションに数値バリデーションを追加
- `check-open-issues.sh` の `gh issue list` 失敗時のエラー処理改善
- `suggest-version.sh` の case 文に default ケースを追加

## 境界
- `check-open-issues.sh` と `suggest-version.sh` 以外のスクリプトは対象外
- スクリプトのインターフェース（引数、出力形式、終了コード）は変更しない

## 依存関係

### 依存する Unit
- なし

### 外部依存
- GitHub CLI (`gh`)

## 非機能要件（NFR）
- **パフォーマンス**: 既存と同等（バリデーション追加による遅延は無視できるレベル）
- **セキュリティ**: 入力バリデーションによる堅牢性向上
- **互換性**: 正常系の既存動作を維持

## 技術的考慮事項
- 修正は `prompts/package/bin/` 配下で行う
- 数値バリデーションは bash の正規表現または算術展開で実装

## 実装優先度
High

## 見積もり
小（修正箇所が明確で限定的）

## 関連Issue
- #194: 諸々調整（項番 #2, #9, #12）

---
## 実装状態

- **状態**: 完了
- **開始日**: 2026-02-18
- **完了日**: 2026-02-19
- **担当**: AI (Claude)

# Unit: issue-ops.sh 認証判定バグ修正

## 概要
issue-ops.sh の check_gh_available() 関数が、認証済み環境でも gh-not-authenticated エラーを返すバグを修正する。

## 含まれるユーザーストーリー
- ストーリー 2: issue-ops.sh認証判定バグの修正 (#225)

## 責務
- check_gh_available() 関数の認証判定ロジック修正
- 認証済み環境での正常動作確認
- gh未インストール/未認証環境での従来動作維持確認

## 境界
- issue-ops.sh の他のサブコマンドのロジック変更は含まない（認証チェック部分のみ）
- 新しいサブコマンドの追加は含まない

## 依存関係

### 依存する Unit
- なし

### 外部依存
- GitHub CLI (gh) v2.0.0 以上（`--hostname` オプションのサポートに必要。未対応バージョンの場合は従来の `gh auth status` にフォールバックする）

## 非機能要件（NFR）
- **パフォーマンス**: 既存と同等
- **セキュリティ**: 認証状態の正確な判定
- **スケーラビリティ**: 該当なし
- **可用性**: 該当なし

## 技術的考慮事項
- `gh auth status --hostname github.com` を使用して特定ホストの認証のみ確認
- 複数ホスト構成（Enterprise等）での誤判定を防止
- `prompts/package/bin/issue-ops.sh` を編集

## 実装優先度
High

## 見積もり
小規模（認証チェック関数の修正）

## 関連Issue
- #225

---
## 実装状態

- **状態**: 未着手
- **開始日**: -
- **完了日**: -
- **担当**: -

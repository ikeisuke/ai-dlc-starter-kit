# Unit: AIエージェント許可リストガイド

## 概要
AIエージェント向けの安全なコマンド許可リストと各ツールでの設定方法を説明するガイドを作成する。

## 含まれるユーザーストーリー
- B-2: AIエージェント許可リストガイド (#29)

## 責務
- ai-agent-allowlist.mdガイドファイルの作成
- 許可リスト推奨コマンド一覧の記載
- 除外すべきコマンド（破壊的・歴史改変）の記載
- 各AIエージェントの設定方法とテンプレート例の提供

## 境界
- サンドボックス環境の詳細は含めない（Unit 004で対応）
- コマンド自体の使用方法は記載しない（許可リスト設定に特化）

## 依存関係

### 依存する Unit
- なし

### 外部依存
- なし

## 非機能要件（NFR）
- **パフォーマンス**: N/A（ドキュメント作成のみ）
- **セキュリティ**: セキュリティガイドとして正確な情報を記載
- **スケーラビリティ**: N/A
- **可用性**: N/A

## 技術的考慮事項
- 作成ファイル: `prompts/package/guides/ai-agent-allowlist.md`
- 必須セクション:
  - 許可リスト推奨コマンド:
    - 読み取り専用: `cat`, `ls`, `find`, `grep`, `head`, `tail` 等
    - 作成系: `mkdir`, `touch`, `echo` 等
    - Git操作: `git status`, `git diff`, `git log`, `git add`, `git commit` 等
  - 除外すべきコマンド: `rm -rf`, `git push --force`, `git reset --hard`, `sudo` 等
  - 各AIエージェントの設定方法:
    - Claude Code: `.claude/settings.json` の `allowedTools` 設定
    - Cursor: `.cursorrules` または設定ファイル
    - Cline: 設定ファイルでの許可リスト
    - Windsurf: 設定ファイルでの許可リスト
    - Aider: 設定ファイルでの許可リスト
  - 設定ファイルテンプレート: 各ツール向けのコピー可能なテンプレート例

## 実装優先度
Medium

## 見積もり
中規模（新規ガイドファイルの作成）

---
## 実装状態

- **状態**: 未着手
- **開始日**: -
- **完了日**: -
- **担当**: -

# Unit 002 計画: PRマージ前レビューの汎用化

## 概要

`operations-release.md` の7.13節（PRマージ前レビュー）から具体的ツール名を除去し、汎用的な記述に変更する。ツール選択はプロジェクト固有ルール（`rules.md`）に委ねる。

## 変更対象ファイル

- `prompts/package/prompts/operations-release.md` (L492-571)

## 実装計画

1. サブステップ1（ローカルレビュー）: `/review` や `Claude Code` 等の具体名を除去し、「AIエージェントのレビュー機能」のような汎用記述に変更
2. サブステップ2（Codexレビュー）: `codex` 固有のコマンドを除去し、「外部AIレビューツール」のような汎用記述に変更。ツール選択は `rules.md` のプロジェクト固有ルールに委ねる旨を記載
3. サブステップ3（完了待機）: Codex固有のフローを汎用化
4. `rules.md` のプロジェクト固有ルールとの役割分離を明確化するコメントを追加

## 完了条件チェックリスト

- [ ] `operations-release.md` の7.13節を汎用化
- [ ] 具体的ツール名（codex, claude, /review等）を除去
- [ ] ツール選択をプロジェクト固有ルールに委ねる記述に変更
- [ ] ファイル不存在時のスキップ記述を `operations-release.md` に明示

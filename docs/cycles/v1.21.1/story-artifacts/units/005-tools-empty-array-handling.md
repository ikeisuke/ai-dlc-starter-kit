# Unit: ツールチェック回避設定

## 概要
`tools = []`（空配列）設定時に外部CLIのwhichチェックをスキップし、セルフレビューモードに直接遷移するロジックをreview-flow.mdに追加する。

## 含まれるユーザーストーリー
- ストーリー6: 環境依存ツールチェックの回避設定（#306）

## 関連Issue
- #306

## 責務
- review-flow.md ステップ3: `tools` が空配列の場合に `cli_available=false` として扱い、外部CLIのwhichチェックをスキップ
- review-flow.md: 空配列時にセルフレビューフローへ直接遷移するロジックの追加
- 既存のエラー処理（配列以外の型→警告＋デフォルト）の維持

## 境界
- aidlc.tomlのスキーマ構造自体の変更は含まない（既存の `tools` キーの解釈変更のみ）
- read-config.shの修正は含まない（既存のマージルール「配列は完全置換」で動作）

## 依存関係

### 依存する Unit
- なし（review-flow.mdの変更は他のUnitに依存しない）

### 外部依存
- なし

## 非機能要件（NFR）
- なし（プロンプトファイルの変更のみ）

## 技術的考慮事項
- `tools = []` は「外部CLIを使用しない」の明示的な意思表示として扱う
- `tools` キー未設定時はデフォルト `["codex"]` で従来動作を維持
- `aidlc.toml.local` の `tools` 設定は `aidlc.toml` を完全上書き（read-config.shの既存マージルール）
- 変更対象は `prompts/package/prompts/common/review-flow.md`（メタ開発ルール）
- Unit 001〜004と並行実装可能

## 実装優先度
Medium

## 見積もり
小（タスク数: review-flow.md 1ファイルの条件分岐追加のみ）

---
## 実装状態

- **状態**: 未着手
- **開始日**: -
- **完了日**: -
- **担当**: -

# Unit 006: セルフアップデート廃止 - 実装計画

## 概要
Operations Phaseの「メタ開発特有の完了時作業」セクションを削除し、通常のセットアップフローでアップデートを行うように変更する。

## 対象ファイル
- `docs/cycles/operations.md`

## 変更内容

### 削除するセクション
「メタ開発特有の完了時作業【重要】」セクション（111行目〜137行目）を削除

削除対象内容:
- バージョン更新手順
- setup-init実行（アップグレードモード）の説明
- 注意書き

### 追加する内容
削除後、以下の簡潔な案内を追加:

```markdown
## アップデート方法

AI-DLC Starter Kit自体のアップデートは、通常のセットアップフローを使用してください:

1. `docs/aidlc/prompts/setup-prompt.md` を読み込み
2. 「既存プロジェクト + アップグレード」を選択
3. セットアップ手順に従う

詳細は setup-prompt.md を参照してください。
```

### 更新履歴の追加
更新履歴に今回の変更を追記

## 設計について
このUnitはドキュメントの削除・簡素化のみであり、コード変更がないため、ドメインモデル設計・論理設計は省略します。

## 作業手順
1. Unit定義ファイルの「実装状態」を「進行中」に更新
2. `docs/cycles/operations.md` を編集
   - 「メタ開発特有の完了時作業【重要】」セクションを削除
   - 「アップデート方法」セクションを追加
   - 更新履歴に追記
3. 実装記録を作成
4. Unit定義ファイルの「実装状態」を「完了」に更新
5. 履歴記録
6. Gitコミット
7. バックログアイテムの削除

## 関連バックログ
- `docs/cycles/backlog/refactor-remove-self-update-from-operations.md`（このUnit完了後に削除）

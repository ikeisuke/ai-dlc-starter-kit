# Unit: 定型コマンドのスクリプト化

## 概要

AI-DLCで頻繁に使用する定型コマンドをスクリプト化し、許可リスト設定を簡素化する。

## 含まれるユーザーストーリー

- C-1: 定型コマンドのスクリプト化 (#142)

## 責務

- `aidlc-env-check.sh`（環境チェック: gh, jj, dasel等の存在確認）を作成
- `aidlc-git-info.sh`（Git状態取得: status, branch, log等）を作成
- `aidlc-cycle-info.sh`（サイクル情報取得: ブランチ名からバージョン抽出等）を作成
- 許可リストガイドにスクリプト活用方法を追記

## 境界

- 既存スクリプト（check-gh-status.sh等）は変更しない
- 同期スクリプト（rsync関連）は対象外

## 依存関係

### 依存する Unit

- なし

### 外部依存

- なし

## 非機能要件（NFR）

- **パフォーマンス**: 各スクリプトは1秒以内に完了
- **セキュリティ**: 外部コマンドの実行結果のみを出力、入力は受け付けない
- **スケーラビリティ**: N/A
- **可用性**: N/A

## 技術的考慮事項

- `prompts/package/bin/` に配置（メタ開発のため）
- 既存スクリプトの命名規則・コーディングスタイルに合わせる
- 実行権限付与を忘れずに

## 実装優先度

Medium

## 見積もり

中（スクリプト3本 + ガイド更新）

---
## 実装状態

- **状態**: 未着手
- **開始日**: -
- **完了日**: -
- **担当**: -

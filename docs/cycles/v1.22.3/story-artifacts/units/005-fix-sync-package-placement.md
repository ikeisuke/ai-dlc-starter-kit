# Unit: sync-package.sh配置見直しとlib同期検証

## 概要

`sync-package.sh` がrsync同期対象ディレクトリ内に配置されている問題の修正と、`lib/` ディレクトリの同期が正しく動作していることの検証。

## 含まれるユーザーストーリー

- ユーザーからの割り込み要望（Construction Phase中に発見）

## 責務

- `sync-package.sh` の配置場所を見直し、rsync対象外に移動する
- `lib/` ディレクトリの同期動作を検証し、問題があれば修正する
- `aidlc-setup.sh` の `sync-package.sh` 参照パスを必要に応じて更新する

## 境界

- `sync-package.sh` の機能自体の変更は最小限に留める
- `aidlc-setup.sh` の同期ロジック（SYNC_DIRS）は変更しない（既にlibが含まれている）

## 依存関係

### 依存する Unit

- なし

### 外部依存

- なし

## 非機能要件（NFR）

- **パフォーマンス**: 影響なし
- **セキュリティ**: 影響なし
- **スケーラビリティ**: 影響なし
- **可用性**: 影響なし

## 技術的考慮事項

- メタ開発環境と外部プロジェクト環境の両方で動作確認が必要
- `aidlc-setup.sh` が `sync-package.sh` を参照するパスの変更に注意

## 実装優先度

High

## 見積もり

小規模

## 関連Issue

- #346

---
## 実装状態

- **状態**: 完了
- **開始日**: 2026-03-17
- **完了日**: 2026-03-17
- **担当**: @ai

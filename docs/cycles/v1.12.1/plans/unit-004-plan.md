# Unit 004 計画: Construction Phase確認の自動化

## 概要

Construction PhaseのUnit完了時確認（AIレビュー実施確認、Operations引き継ぎ確認）をユーザーへの質問形式からAI自身による自動確認に変更する。

## 関連Issue

- #156

## 変更対象ファイル

- `prompts/package/prompts/construction.md` - Construction Phaseプロンプト

**注意**: メタ開発ルールに従い、`docs/aidlc/prompts/construction.md`ではなく`prompts/package/prompts/construction.md`を編集する。

## 自動確認の基本方針

### 変数の決定方法

- `{{CYCLE}}`: ブランチ名 `cycle/vX.X.X` から取得（既存のサイクル判定ロジックを使用）
- `{NN}`: 現在作業中のUnit番号（2桁ゼロパディング）。Unit定義ファイル名から取得

### 0.6 AIレビュー実施確認

- **参照ファイル**: `docs/cycles/{{CYCLE}}/history/construction_unit{NN}.md`
- **検出パターン**: 履歴内に「AIレビュー」または「レビュー反映」を含む記録があるか
- **判定ロジック**:
  - 履歴ファイルが存在しない → 未実施扱いで警告（「履歴ファイルが見つかりません」）
  - 記録あり → AIレビュー実施済みと判断
  - 記録なし → 未実施として警告を出力し、対応を提示

### 0.7 Operations引き継ぎ確認

- **参照ディレクトリ**: `docs/cycles/{{CYCLE}}/operations/tasks/`
- **検出条件**: ディレクトリ内に`.md`ファイルが1件以上存在するか
- **判定ロジック**:
  - ファイルあり → 引き継ぎタスクありと判断し、ファイル名の一覧を表示（タイトル抽出は行わない）
  - ファイルなし → 引き継ぎタスクなしと判断

## 実装計画

### Phase 1: 設計

1. **ドメインモデル設計**: 自動確認ロジックの設計
   - AIレビュー実施確認: 履歴ファイルからの検出パターン
   - 引き継ぎ確認: ディレクトリ存在チェックの仕様

2. **論理設計**: プロンプト変更の詳細設計
   - 0.6セクションの変更内容
   - 0.7セクションの変更内容

### Phase 2: 実装

3. **コード生成**: construction.mdの修正
4. **テスト**: 動作確認（以下のケースを確認）
   - 履歴あり + tasksあり
   - 履歴あり + tasksなし
   - 履歴なし + tasksあり
   - 履歴なし + tasksなし

## 完了条件チェックリスト

- [ ] AIレビュー実施確認: 履歴ファイル（`history/construction_unit{NN}.md`）を読んで自動判断するように変更
- [ ] 引き継ぎ確認: `operations/tasks/`ディレクトリ内の`.md`ファイル存在で自動判断するように変更
- [ ] ユーザーへの質問形式が削除され、自動判断結果の出力形式に変更される

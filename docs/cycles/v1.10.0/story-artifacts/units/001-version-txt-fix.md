# Unit: version.txt参照問題修正

## 概要
`prompts/version.txt` が存在しないのに参照されている問題を修正する。バージョン情報の参照エラーを解消し、セットアップ・アップグレード時の安定性を向上させる。

## 含まれるユーザーストーリー
- ストーリー 1: version.txtの参照問題修正 (#126)

## 責務
- `prompts/version.txt` を参照している箇所の特定
- 参照箇所の修正または `prompts/version.txt` の作成
- 修正後の動作確認

## 境界
- バージョンチェック機能の新規追加は Unit 003 で対応
- `docs/aidlc/` 配下のファイルは直接編集しない

## 依存関係

### 依存する Unit
- なし（最初に実施すべきUnit）

### 外部依存
- なし

## 非機能要件（NFR）
- **パフォーマンス**: 該当なし
- **セキュリティ**: 該当なし
- **スケーラビリティ**: 該当なし
- **可用性**: セットアップ・アップグレード時にエラーが発生しないこと

## 技術的考慮事項
- 調査対象: `prompts/` 配下、`prompts/package/bin/` 配下のスクリプト
- 修正対象: `prompts/` 配下（`prompts/version.txt` 作成を含む）、`prompts/package/` 配下
- `docs/aidlc/` 配下は直接編集しない
- バージョン情報の取得元を統一（`version.txt` または `docs/aidlc.toml`）
- AIレビュー指摘: 「存在 or 参照修正」のどちらを採用するかは調査結果に基づいて判断

## 実装優先度
High（最優先バグ修正）

## 見積もり
小規模（調査・修正）

---
## 実装状態

- **状態**: 未着手
- **開始日**: -
- **完了日**: -
- **担当**: -

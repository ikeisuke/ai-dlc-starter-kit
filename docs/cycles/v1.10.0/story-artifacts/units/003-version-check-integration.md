# Unit: バージョンチェック機能統合

## 概要
環境一覧スクリプトにバージョンチェック機能を組み込み、環境情報とバージョン情報を一括で確認できるようにする。

## 含まれるユーザーストーリー
- ストーリー 2: バージョンチェック機能の統合 (#128)

## 責務
- 環境確認スクリプトへのバージョン出力機能追加
- `version.txt` からのバージョン情報取得

## 境界
- 新規ヘルパースクリプトの作成は許可（バージョンチェック専用）
- バージョン情報の表示フォーマットは既存の出力形式に合わせる

## 依存関係

### 依存する Unit
- Unit 001: version.txt参照問題修正（依存理由: version.txtの参照が正常に動作する必要があるため）

### 外部依存
- なし

## 非機能要件（NFR）
- **パフォーマンス**: スクリプト実行時間への影響を最小限に
- **セキュリティ**: 該当なし
- **スケーラビリティ**: 該当なし
- **可用性**: 該当なし

## 技術的考慮事項
- 対象スクリプト: `prompts/package/bin/` 配下の環境確認系スクリプト

**AIレビュー指摘への対応（Unit定義で特定）**:
- 具体的な対象スクリプト候補:
  - `check-env.sh` - 環境一覧表示
  - `check-gh-status.sh` - GitHub CLI状態確認
  - または新規の `check-version.sh` を作成し、既存スクリプトから呼び出し
- 出力形式: `version:X.X.X` または `starter_kit_version:X.X.X`

## 実装優先度
Medium

## 見積もり
小規模（スクリプト修正）

---
## 実装状態

- **状態**: 未着手
- **開始日**: -
- **完了日**: -
- **担当**: -

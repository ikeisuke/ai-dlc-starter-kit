# 実装記録: 簡略指示「AIDLCアップデート」追加

## Unit情報

- **Unit名**: 簡略指示「AIDLCアップデート」追加
- **Unit番号**: 008
- **関連Issue**: #69

## 実装概要

AGENTS.mdのフェーズ簡略指示テーブルに「AIDLCアップデート」の指示パターンを追加し、ユーザーが簡単な指示で環境更新を開始できるようにした。

## 変更ファイル

| ファイル | 変更内容 |
|----------|----------|
| `prompts/package/prompts/AGENTS.md` | 簡略指示テーブルに新規行追加、列名変更 |

## 実装詳細

### 変更内容

1. **列名変更**: 「対応フェーズ」→「対応処理」（アップグレードはフェーズではないため）
2. **新規行追加**: `「AIDLCアップデート」「update aidlc」「start upgrade」 | アップグレード（環境更新）`
3. **Lite版テーブル**: 列名を「対応処理」に統一

### setupとupgradeの区別

| 指示 | 目的 | 読み込むファイル |
|------|------|-----------------|
| `start setup` / 「セットアップ」 | 新規サイクル開始 | `docs/aidlc/prompts/setup.md` |
| `start upgrade` / 「AIDLCアップデート」 | 環境更新 | `prompts/setup-prompt.md` |

## テスト結果

- N/A（ドキュメント変更のためテスト不要）

## コードレビュー結果

- [x] AIレビュー（Codex）: 3回実施、指摘事項修正済み
- [x] Markdownlint: パス
- [x] 設計との整合性: 確認済み

## 技術的な決定事項

- 「AIDLC」表記（ハイフンなし）は意図的な選択。ユーザー入力の利便性を優先

## 完了ステータス

- **状態**: 完了
- **完了日**: 2026-01-17

# Unit: mktemp 許可ルールの統合

## 概要
`.claude/settings.json` の mktemp 関連許可ルール5件をワイルドカード1件に統合し、`setup-ai-tools.sh` のテンプレート生成も同時に更新する。

## 含まれるユーザーストーリー
- ストーリー 2: mktemp 許可ルールの統合

## 責務
- `.claude/settings.json` の mktemp 個別許可ルール5件をワイルドカードパターン1件に統合
- `setup-ai-tools.sh` の `_generate_template` 関数の更新

## 境界
- mktemp 以外の許可ルールは変更しない
- `.claude/settings.json` の他の設定項目には触れない

## 依存関係

### 依存する Unit
- なし

### 外部依存
- `.claude/settings.json` のパターンマッチ仕様（実装前にワイルドカード構文の動作確認が必要）

### 前提条件
- Claude Code の許可パターンが `Bash(mktemp /tmp/aidlc-:*)` 形式のワイルドカードをサポートしていること（サポートしない場合は代替パターンを検討）

## 非機能要件（NFR）
- **パフォーマンス**: 該当なし
- **セキュリティ**: ワイルドカードパターンが `/tmp/aidlc-` プレフィックスに限定され、権限の過剰拡大がないこと
- **スケーラビリティ**: 該当なし
- **可用性**: 該当なし

## 技術的考慮事項
- ワイルドカード構文の確認が必要（`Bash(mktemp /tmp/aidlc-:*)` 形式）
- `setup-ai-tools.sh` の既存テンプレートとの整合性確認

## 実装優先度
Medium

## 見積もり
小規模（設定ファイル編集 + スクリプト更新）

## 関連Issue
- #383

---
## 実装状態

- **状態**: 未着手
- **開始日**: -
- **完了日**: -
- **担当**: -

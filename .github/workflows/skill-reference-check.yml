name: Skill Reference Check

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, edited]
    branches: [main]

permissions:
  contents: read

jobs:
  skill-reference-check:
    name: Skill Reference Check
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Detect skip
        id: detect
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_DRAFT: ${{ github.event.pull_request.draft }}
          PATHS_REGEX: '^(skills/.+|bin/check-skill-references\.sh|bin/check-bash-substitution\.sh|bin/check-test-isolation\.sh|bin/check-test-isolation\.allowlist|bin/tests/check-test-isolation/.*\.bats|bin/check-frontmatter-parse-guard\.sh|bin/tests/check-frontmatter-parse-guard\.sh|tests/.*\.bats|\.github/workflows/skill-reference-check\.yml)$'
        run: |
          set -eu
          if [ "$PR_DRAFT" = "true" ]; then
            printf 'should_skip=true\n' >> "$GITHUB_OUTPUT"
            echo "Skip reason: Draft PR"
            exit 0
          fi
          gh api "repos/${GITHUB_REPOSITORY}/pulls/${PR_NUMBER}/files" --paginate --jq '.[].filename' > "$RUNNER_TEMP/changed-files.txt"
          if grep -E "$PATHS_REGEX" "$RUNNER_TEMP/changed-files.txt" >/dev/null; then
            printf 'should_skip=false\n' >> "$GITHUB_OUTPUT"
            echo "Skip reason: none (paths matched)"
          else
            printf 'should_skip=true\n' >> "$GITHUB_OUTPUT"
            echo "Skip reason: paths not match"
          fi

      - name: Checkout
        if: steps.detect.outputs.should_skip != 'true'
        uses: actions/checkout@v4

      - name: Check skill references for direct path violations
        if: steps.detect.outputs.should_skip != 'true'
        run: bash bin/check-skill-references.sh

      - name: Check bash substitution (command substitution in markdown bash blocks)
        if: steps.detect.outputs.should_skip != 'true'
        run: bash bin/check-bash-substitution.sh

      - name: Check test isolation (BATS cwd dependency patterns)
        if: steps.detect.outputs.should_skip != 'true'
        run: bash bin/check-test-isolation.sh

      - name: Check frontmatter parse guard (forbidden parse patterns in aidlc consumers)
        if: steps.detect.outputs.should_skip != 'true'
        run: bash bin/check-frontmatter-parse-guard.sh

      - name: Run frontmatter parse guard tests
        if: steps.detect.outputs.should_skip != 'true'
        run: bash bin/tests/check-frontmatter-parse-guard.sh
